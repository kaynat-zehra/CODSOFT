# Password generator that takes an input for desired length and generates a random password of the required length.

import random


print("                 Password Generator                 ")
length = int(input ("Please Enter the desired length of password : "))
password = ""
for i in range(length):
    n = random.randint(33, 126)
    password+= chr(n)

print("The random generated password of your desired length is : ", password)
